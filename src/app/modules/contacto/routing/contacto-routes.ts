import { RouteContext } from "src/shared/model/routing/route-context";

export class ContactoRoutes {
    static readonly contactanos: RouteContext = {
        uri: '/',
        ucName: 'Pantalla con formulario de contacto al correo de bb'
    }
}